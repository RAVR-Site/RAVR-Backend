# Database connection string
DATABASE_DSN=host=localhost user=postgres password=postgres dbname=echo_mvp port=5432 sslmode=disable

# JWT secret key (use strong secret in production!)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# JWT access token expiration in hours (default: 24)
JWT_ACCESS_EXPIRATION=24

# Server port
PORT=8080
